import 'package:bbarna/units/widgets/unit_form.dart';
import 'package:flutter/material.dart';

/// Add Unit screen — the shared [UnitForm] in "add" mode.
class AddUnit extends StatelessWidget {
  const AddUnit({super.key});

  @override
  Widget build(BuildContext context) => const UnitForm();
}
