import 'package:bbarna/live_class/widgets/live_class_form.dart';
import 'package:flutter/material.dart';

/// Add Class screen — the shared [LiveClassForm] in "add" mode.
class AddLiveClass extends StatelessWidget {
  const AddLiveClass({super.key});

  @override
  Widget build(BuildContext context) => const LiveClassForm();
}
