import 'package:bbarna/documents/audio/widgets/audio_form.dart';
import 'package:flutter/material.dart';

/// Add Audio screen — the shared [AudioForm] in "add" mode.
class AddAudio extends StatelessWidget {
  const AddAudio({super.key});

  @override
  Widget build(BuildContext context) => const AudioForm();
}
