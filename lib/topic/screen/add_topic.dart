import 'package:bbarna/topic/widgets/topic_form.dart';
import 'package:flutter/material.dart';

/// Add Topic screen — the shared [TopicForm] in "add" mode.
class AddTopic extends StatelessWidget {
  const AddTopic({super.key});

  @override
  Widget build(BuildContext context) => const TopicForm();
}
